
"""
water is a:

* Convinient scripting language with an unintrusive type system that does not fall apart as your script grows.
Pulls power from python and bash instead of trying to reinvent the wheel.

Features:
    * Embeds python runtime enabling support for python libraries
    * Embeds bash runtime enabling support for inline shell scripting
    * Static typing which only shows up if you want it to
    * Minimal syntax with only 30 keywords
"""

let k = 444
let m = 43
let o = 99
print o

hello = (a: int, b: int) => int
    for i in a..b
        print(i)

    thing = Thing{1, 2, 3}
    thang = Thang.thing

    dirpath = "/bin/bash"

    $ls dirpath

    
    for file in $(ls {dirpath} | sort).split(" ")

    thing.a

Thing = {
    thing: int = 5
    thing: str
    thing: str

    hello = (a: int, b: int) => int
        print(a)

        return b
}

Thang = {
    thing
    thing
    thing
}
    

